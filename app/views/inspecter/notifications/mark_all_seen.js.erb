var notificationCount = <%= @notifications.not_seen.count %>;

$("#notifications-list").html("<%= escape_javascript(render(@notifications)) %>");
$("#notification-menu-title").html('Notifications ' + '(' + notificationCount + ')');
